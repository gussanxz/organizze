<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Fundo transparente -->
    <stroke
        android:width="2dp"
        android:color="#FF0000" /> <!-- Cor da borda (vermelho nesse exemplo) -->
    <corners android:radius="8dp" /> <!-- Opcional: deixa a borda arredondada -->
</shape>
